import { TextValuePipe } from './text-value.pipe';

describe('TextValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TextValuePipe();
    expect(pipe).toBeTruthy();
  });
});
