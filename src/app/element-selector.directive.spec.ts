import { ElementSelectorDirective } from './element-selector.directive';

describe('ElementSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
